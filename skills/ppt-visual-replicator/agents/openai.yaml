interface:
  display_name: "PPT Visual Replicator"
  short_description: "Preserve native PPTs or recreate them accurately"
  default_prompt: "Use $ppt-visual-replicator to inspect this target PPTX first; preserve it if it is already natively editable, otherwise use original-page screenshot → built-in imagegen redraw → PNG → editable PPTX and require a Quick Look original-source visual gate for every page."
